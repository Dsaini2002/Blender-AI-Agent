## What does this PR do?

## Checklist
- [ ] `python run_tests.py` passes locally
- [ ] New `Tool`/`Skill`/`Provider` has tests using `FakeBridge`/`MockProvider`
- [ ] README updated if this adds a phase-level capability
- [ ] No API keys, secrets, or `.blend` files committed
- [ ] Benchmark suite still passes (if touching `agent/`, `reliability/`, or `tools/`)